# PayMyDine Frontend V2 release example — never put secrets in this file.
NODE_ENV=production
PORT=3002

# The integrated V2 application uses same-origin Laravel proxy routes.
# Production tenant/payment secrets stay in the Laravel tenant configuration,
# not in NEXT_PUBLIC_* variables or this release package.
