/*
 * DEPRECATED: PMD_TURKISH_ADMIN_I18N_R3
 *
 * Turkish copy is now server/key based and owned exclusively by
 * app/admin/i18n/platform/tr.php. This file intentionally performs no DOM
 * mutation, text guessing, MutationObserver translation, or locale override.
 *
 * It remains as a temporary no-op because older shared layouts may still load
 * this asset. Once all deployed layouts are on the master-catalogue build the
 * script tag can be removed without changing translation behaviour.
 */
(function () {
    'use strict';
})();
