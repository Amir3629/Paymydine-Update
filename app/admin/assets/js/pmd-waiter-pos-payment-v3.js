(function () {
  'use strict';

  if (window.PMDWaiterPOSPaymentV2 && window.PMDWaiterPOSPaymentV2.__pmdV3) return;

  window.PMDWaiterPOSPaymentV2 = {
    __pmdV3: true,
    install: function (ctx) {
      var state=ctx.state,$=ctx.$,$$=ctx.$$,esc=ctx.esc,uid=ctx.uid,toNumber=ctx.toNumber,roundMoney=ctx.roundMoney,replaceOrderToken=ctx.replaceOrderToken,money=ctx.money,fetchJson=ctx.fetchJson,toast=ctx.toast,showSuccess=ctx.showSuccess,refreshData=ctx.refreshData;
      function paymentSummaryUrl(){return replaceOrderToken(state.settings.payment_summary_url,state.activeOrderId)}
      function paymentSettleUrl(){return replaceOrderToken(state.settings.payment_settle_url,state.activeOrderId)}
      function paymentCouponUrl(){return replaceOrderToken(state.settings.payment_coupon_url,state.activeOrderId)}
      function terminalPaymentUrl(){return replaceOrderToken(state.settings.terminal_payment_url,state.activeOrderId)}
      function terminalAttemptRefreshUrl(id){return '/admin/terminal-payments/attempts/'+encodeURIComponent(String(id))+'/refresh'}
      function terminalAttemptsUrl(){return '/admin/orders/'+encodeURIComponent(String(state.activeOrderId))+'/terminal-payment-attempts'}
      function wait(ms){return new Promise(function(resolve){setTimeout(resolve,ms)})}
      function jsonHeaders(){return {'Accept':'application/json','Content-Type':'application/json','X-Requested-With':'XMLHttpRequest','X-CSRF-TOKEN':(($('meta[name="csrf-token"]',document)||{}).content||'')}}

      function resetPaymentState(){state.payment.loading=false;state.payment.submitting=false;state.payment.splitMode='full';state.payment.equalPeople=Math.max(2,Math.min(10,state.guestCount||2));state.payment.customAmount='';state.payment.itemQuantities={};state.payment.method='cash';state.payment.providerCode=null;state.payment.terminalDeviceId=null;state.payment.terminalAttemptId=null;state.payment.tipPercent=0;state.payment.customTip='';state.payment.coupon=null;state.payment.couponCode='';state.payment.payerLabel='';state.payment.reference='';state.payment.cashReceived='';state.payment.externalConfirmed=false;state.payment.idempotencyKey=uid('pay')}
      async function openPayment(){if(!state.activeOrderId)return toast('Save the order before taking payment.',true);if(state.cart&&state.cart.length)return toast('Save new items before taking payment.',true);resetPaymentState();var modal=$('[data-pos-payment-modal]');if(!modal)return;modal.classList.add('is-show');modal.setAttribute('aria-hidden','false');state.payment.open=true;await loadPaymentSummary(true)}
      function closePayment(){var modal=$('[data-pos-payment-modal]');if(modal){modal.classList.remove('is-show','is-direct-terminal','is-online-payment');modal.setAttribute('aria-hidden','true')}state.payment.open=false}
      async function loadPaymentSummary(silent){if(!state.activeOrderId||state.payment.loading)return;state.payment.loading=true;try{var summary=await fetchJson(paymentSummaryUrl()+'?_='+Date.now());state.payment.summary=summary;var remaining=paymentRemaining();if(state.payment.cashReceived==='')state.payment.cashReceived=roundMoney(remaining).toFixed(2);renderPayment();if(!silent)toast('Payment status updated')}catch(error){toast(error.message||'Could not load payment details.',true)}finally{state.payment.loading=false;renderPayment()}}
      function paymentRemaining(){return toNumber(state.payment.summary&&state.payment.summary.settlement&&state.payment.summary.settlement.remaining_amount,0)}
      function selectedItemPayload(){var summary=state.payment.summary||{};return(summary.items||[]).reduce(function(rows,item){var quantity=toNumber(state.payment.itemQuantities[String(item.order_menu_id)],0);if(quantity>0)rows.push({order_menu_id:Number(item.order_menu_id),quantity:quantity});return rows},[])}
      function paymentBaseAmount(){var remaining=paymentRemaining();if(state.payment.method==='direct_terminal')return remaining;if(state.payment.splitMode==='full')return remaining;if(state.payment.splitMode==='equal')return roundMoney(remaining/Math.max(2,state.payment.equalPeople));if(state.payment.splitMode==='custom')return Math.min(remaining,Math.max(0,toNumber(state.payment.customAmount,0)));if(state.payment.splitMode==='items'){var summary=state.payment.summary||{},ratio=toNumber(summary.settlement&&summary.settlement.gross_ratio,1);return roundMoney((summary.items||[]).reduce(function(sum,item){var quantity=toNumber(state.payment.itemQuantities[String(item.order_menu_id)],0);return sum+toNumber(item.unit_price,0)*quantity*ratio},0))}return remaining}
      function paymentTipAmount(){if(state.payment.method==='direct_terminal')return 0;var base=paymentBaseAmount();if(state.payment.tipPercent==='custom')return Math.max(0,roundMoney(state.payment.customTip));return roundMoney(base*(toNumber(state.payment.tipPercent,0)/100))}
      function couponDiscount(){return state.payment.method==='direct_terminal'?0:(state.payment.coupon?toNumber(state.payment.coupon.discount,0):0)}
      function paymentPayable(){return roundMoney(Math.max(0,paymentBaseAmount()+paymentTipAmount()-couponDiscount()))}

      function renderPaymentBalance(){var summary=state.payment.summary,container=$('[data-pos-payment-balance]'),subtitle=$('[data-pos-payment-subtitle]');if(!container)return;if(!summary){container.innerHTML='<div class="pmd-pos-payment-history-empty">Loading…</div>';return}var settlement=summary.settlement||{};container.innerHTML='<div class="pmd-pos-balance-card is-remaining pmd-pos-balance-hero"><span>Amount due</span><b>'+money(settlement.remaining_amount)+'</b><small>Order total '+money(settlement.order_total)+' · Paid '+money(settlement.settled_amount)+'</small></div>';if(subtitle)subtitle.textContent='Order #'+summary.order.order_id+' · '+(summary.table?summary.table.name:'Table order')}
      function renderSplitPanel(){var panel=$('[data-pos-split-panel]'),summary=state.payment.summary;if(!panel||!summary)return;if(state.payment.method==='direct_terminal'){state.payment.splitMode='full';panel.innerHTML='';return}$$('[data-split-mode]').forEach(function(button){button.classList.toggle('is-active',button.dataset.splitMode===state.payment.splitMode)});var remaining=paymentRemaining();if(state.payment.splitMode==='full'){panel.innerHTML='<div class="pmd-pos-split-equal"><div><strong>Full balance</strong><small>Pay everything remaining on this order.</small></div><b>'+money(remaining)+'</b></div>'}else if(state.payment.splitMode==='equal'){panel.innerHTML='<div class="pmd-pos-split-equal"><div><strong>One of '+state.payment.equalPeople+' equal shares</strong><small>Reopen payment for the next payer.</small></div><div class="pmd-pos-split-stepper"><button type="button" data-equal-minus>−</button><b>'+state.payment.equalPeople+'</b><button type="button" data-equal-plus>+</button></div></div>';var minus=$('[data-equal-minus]',panel),plus=$('[data-equal-plus]',panel);if(minus)minus.onclick=function(){state.payment.equalPeople=Math.max(2,state.payment.equalPeople-1);renderPayment()};if(plus)plus.onclick=function(){state.payment.equalPeople=Math.min(10,state.payment.equalPeople+1);renderPayment()}}else if(state.payment.splitMode==='items'){var unpaid=(summary.items||[]).filter(function(item){return toNumber(item.unpaid_quantity,0)>0});panel.innerHTML='<div class="pmd-pos-split-items">'+unpaid.map(function(item){var key=String(item.order_menu_id),value=state.payment.itemQuantities[key]||0;return '<label class="pmd-pos-split-item"><span class="pmd-pos-split-item-name">'+esc(item.name)+'<small>'+esc(item.unpaid_quantity)+' available · '+money(item.unit_price)+'</small></span><input class="pmd-pos-item-pay-qty" data-pay-item="'+esc(key)+'" type="number" min="0" max="'+esc(item.unpaid_quantity)+'" step="1" value="'+esc(value)+'"><b>'+money(toNumber(value,0)*toNumber(item.unit_price,0))+'</b></label>'}).join('')+'</div>';$$('[data-pay-item]',panel).forEach(function(input){input.oninput=function(){var item=unpaid.find(function(row){return String(row.order_menu_id)===String(input.dataset.payItem)}),max=item?toNumber(item.unpaid_quantity,0):0;state.payment.itemQuantities[String(input.dataset.payItem)]=Math.max(0,Math.min(max,toNumber(input.value,0)));renderPaymentTotals()}})}else{panel.innerHTML='<div class="pmd-pos-custom-row"><label><span class="pmd-pos-payment-block-title"><b>Amount</b><span>Max '+money(remaining)+'</span></span><input type="number" min="0.01" max="'+esc(remaining)+'" step="0.01" class="pmd-pos-payment-input" data-custom-payment value="'+esc(state.payment.customAmount)+'" placeholder="0.00"></label><b>'+money(paymentBaseAmount())+'</b></div>';var custom=$('[data-custom-payment]',panel);if(custom)custom.oninput=function(){state.payment.customAmount=custom.value;renderPaymentTotals()}}}

      function renderMethods(){var summary=state.payment.summary,container=$('[data-pos-methods]'),onlineBox=$('[data-pos-online-box]'),terminalBox=$('[data-pos-terminal-box]');if(!summary||!container)return;var methods=[{key:'cash',name:'Cash',note:'Cash payment'},{key:'external_terminal',name:'Manual terminal',note:'Record an already approved terminal payment'}];if((summary.terminal_providers||[]).length)methods.splice(1,0,{key:'direct_terminal',name:'Card terminal',note:'Send payment to a connected device'});(summary.methods||[]).forEach(function(method){if(method.code==='cash')return;methods.push({key:'online:'+method.code,name:method.name,note:'Customer checkout',provider:method.provider_code,code:method.code})});container.innerHTML=methods.map(function(method){return '<button type="button" class="pmd-pos-method '+(state.payment.method===method.key?'is-active':'')+'" data-payment-method="'+esc(method.key)+'" data-provider="'+esc(method.provider||'')+'"><b>'+esc(method.name)+'</b><small>'+esc(method.note)+'</small></button>'}).join('');$$('[data-payment-method]',container).forEach(function(button){button.onclick=function(){state.payment.method=button.dataset.paymentMethod;state.payment.providerCode=button.dataset.provider||null;state.payment.terminalDeviceId=null;state.payment.terminalAttemptId=null;state.payment.idempotencyKey=uid('pay');if(state.payment.method==='direct_terminal'){state.payment.splitMode='full';state.payment.tipPercent=0;state.payment.coupon=null}renderPayment()}});var online=state.payment.method.indexOf('online:')===0;if(onlineBox){onlineBox.hidden=!online;if(online)onlineBox.innerHTML='<b>Customer checkout</b><br>Open the secure payment page for the customer.'}if(terminalBox){terminalBox.hidden=state.payment.method!=='direct_terminal';if(state.payment.method==='direct_terminal'){var providers=summary.terminal_providers||[];if(providers.length&&(!state.payment.providerCode||!state.payment.terminalDeviceId)){var firstOnline=providers.find(function(row){return String(row.terminal_status||'').toLowerCase()==='online'})||providers[0];state.payment.providerCode=firstOnline.provider_code;state.payment.terminalDeviceId=firstOnline.terminal_device_id||null}terminalBox.innerHTML='<div class="pmd-pos-terminal-title"><b>'+(providers.length>1?'Choose terminal':'Terminal')+'</b><span>'+(providers.length>1?'Select where the customer will pay':'Ready for card payment')+'</span></div><div class="pmd-pos-terminal-provider-row">'+providers.map(function(provider){var id=provider.terminal_device_id||'',status=String(provider.terminal_status||'unknown').toLowerCase(),onlineStatus=status==='online',active=state.payment.providerCode===provider.provider_code&&String(state.payment.terminalDeviceId||'')===String(id);return '<button type="button" data-terminal-provider="'+esc(provider.provider_code)+'" data-terminal-device-id="'+esc(id)+'" class="'+(active?'is-active ':'')+(onlineStatus?'is-online':'is-offline')+'"><span>'+esc(provider.name)+'</span><small><i></i>'+esc(onlineStatus?'Online':status)+'</small></button>'}).join('')+'</div>';$$('[data-terminal-provider]',terminalBox).forEach(function(button){button.onclick=function(){state.payment.providerCode=button.dataset.terminalProvider;state.payment.terminalDeviceId=button.dataset.terminalDeviceId?Number(button.dataset.terminalDeviceId):null;renderPayment()}})}}var referenceField=$('[data-pos-reference-field]'),externalConfirm=$('[data-pos-external-confirm-row]'),cashField=$('[data-pos-cash-field]');if(referenceField)referenceField.hidden=state.payment.method!=='external_terminal';if(externalConfirm)externalConfirm.hidden=state.payment.method!=='external_terminal';if(cashField)cashField.hidden=state.payment.method!=='cash'}

      function renderAdjustments(){$$('[data-tip-percent]').forEach(function(button){button.classList.toggle('is-active',String(button.dataset.tipPercent)===String(state.payment.tipPercent))});var custom=$('[data-pos-custom-tip]');if(custom){custom.hidden=state.payment.tipPercent!=='custom';custom.value=state.payment.customTip}var code=$('[data-pos-coupon-code]');if(code&&code.value!==state.payment.couponCode)code.value=state.payment.couponCode;var result=$('[data-pos-coupon-result]');if(result)result.textContent=state.payment.coupon?state.payment.coupon.code+' applied: −'+money(state.payment.coupon.discount):'';var payer=$('[data-pos-payer-label]');if(payer&&payer.value!==state.payment.payerLabel)payer.value=state.payment.payerLabel;var reference=$('[data-pos-payment-reference]');if(reference&&reference.value!==state.payment.reference)reference.value=state.payment.reference;var cash=$('[data-pos-cash-received]');if(cash&&document.activeElement!==cash&&cash.value!==state.payment.cashReceived)cash.value=state.payment.cashReceived;var confirm=$('[data-pos-external-confirm]');if(confirm)confirm.checked=state.payment.externalConfirmed}
      function renderPaymentTotals(){var container=$('[data-pos-payment-totals]'),payButton=$('[data-pos-pay-button]'),changeBox=$('[data-pos-change-box]'),copy=$('[data-pos-copy-link]'),refresh=$('[data-pos-refresh-payment]');if(!container)return;var base=paymentBaseAmount(),tip=paymentTipAmount(),coupon=couponDiscount(),payable=paymentPayable();container.innerHTML=state.payment.method==='direct_terminal'?'<div class="pmd-pos-pay-totals is-simple"><div class="pmd-pos-pay-total-row is-grand"><span>Charge</span><strong>'+money(payable)+'</strong></div></div>':'<div class="pmd-pos-pay-totals"><div class="pmd-pos-pay-total-row"><span>Order balance</span><strong>'+money(base)+'</strong></div>'+(tip>0?'<div class="pmd-pos-pay-total-row"><span>Tip</span><strong>'+money(tip)+'</strong></div>':'')+(coupon>0?'<div class="pmd-pos-pay-total-row"><span>Coupon</span><strong>−'+money(coupon)+'</strong></div>':'')+'<div class="pmd-pos-pay-total-row is-grand"><span>Collect now</span><strong>'+money(payable)+'</strong></div></div>';var cashReceived=toNumber(state.payment.cashReceived,0),change=Math.max(0,roundMoney(cashReceived-payable));if(changeBox){changeBox.hidden=state.payment.method!=='cash'||cashReceived<payable;if(!changeBox.hidden)changeBox.textContent='Change: '+money(change)}if(copy)copy.hidden=state.payment.method.indexOf('online:')!==0;if(refresh)refresh.hidden=!state.payment.terminalAttemptId;if(payButton){if(state.payment.method.indexOf('online:')===0)payButton.textContent='Open customer checkout';else if(state.payment.method==='direct_terminal')payButton.textContent='Charge '+money(payable);else if(state.payment.method==='cash')payButton.textContent='Record cash';else payButton.textContent='Record payment';var canCollect=state.payment.summary&&state.payment.summary.permissions&&state.payment.summary.permissions.can_collect_payment,valid=base>0&&payable>0&&!state.payment.submitting;if(state.payment.method!=='direct_terminal'&&state.payment.method.indexOf('online:')!==0&&!canCollect)valid=false;if(state.payment.method==='cash'&&cashReceived+.001<payable)valid=false;if(state.payment.method==='external_terminal'&&(!state.payment.externalConfirmed||!state.payment.reference.trim()))valid=false;if(state.payment.method==='direct_terminal'&&(!state.payment.providerCode||!state.payment.terminalDeviceId))valid=false;payButton.disabled=!valid}}
      function renderPaymentHistory(){var summary=state.payment.summary,container=$('[data-pos-payment-history]'),count=$('[data-pos-payment-history-count]'),wrap=container?container.closest('.pmd-pos-payment-history-wrap'):null;if(!container||!summary)return;var rows=summary.transactions||[];if(wrap)wrap.hidden=!rows.length;if(count)count.textContent=rows.length?rows.length+' payment'+(rows.length===1?'':'s'):'';if(!rows.length){container.innerHTML='';return}container.innerHTML=rows.map(function(tx){return '<article class="pmd-pos-payment-history-item"><div><b>'+esc(String(tx.payment_method||'').replace(/_/g,' '))+' · '+money(tx.amount)+'</b><a href="'+esc(tx.receipt_url||'#')+'" target="_blank" rel="noopener">Receipt</a></div><small>'+esc(tx.paid_at||'')+'</small></article>'}).join('')}
      function renderPayment(){if(!state.payment.open)return;var modal=$('[data-pos-payment-modal]');if(modal){modal.classList.toggle('is-direct-terminal',state.payment.method==='direct_terminal');modal.classList.toggle('is-online-payment',state.payment.method.indexOf('online:')===0)}renderPaymentBalance();if(!state.payment.summary)return;renderSplitPanel();renderMethods();renderAdjustments();renderPaymentTotals();renderPaymentHistory()}

      async function applyCoupon(){var input=$('[data-pos-coupon-code]'),result=$('[data-pos-coupon-result]'),code=String(input?input.value:'').trim().toUpperCase();state.payment.couponCode=code;state.payment.coupon=null;if(!code)return renderPaymentTotals();if(Math.abs(paymentBaseAmount()-paymentRemaining())>.02){if(result)result.textContent='Coupon requires the full remaining balance.';return}try{var json=await fetchJson(paymentCouponUrl(),{method:'POST',headers:jsonHeaders(),body:JSON.stringify({code:code,amount:paymentBaseAmount()})});state.payment.coupon=json;renderPayment()}catch(error){if(result)result.textContent=error.message||'Coupon is invalid.'}}
      function onlineMethodCode(){return state.payment.method.indexOf('online:')===0?state.payment.method.split(':').slice(1).join(':'):null}
      function guestCheckoutUrl(){var summary=state.payment.summary;if(!summary||!summary.guest_checkout_url)return null;var url=new URL(summary.guest_checkout_url,location.origin),method=onlineMethodCode();if(method)url.searchParams.set('payment_method',method);url.searchParams.set('source','waiter_pos');return url.toString()}
      async function copyPaymentLink(){var url=guestCheckoutUrl();if(!url)return toast('Customer payment link is unavailable.',true);try{await navigator.clipboard.writeText(url);toast('Payment link copied')}catch(error){window.prompt('Copy payment link:',url)}}

      async function executePayment(){if(!state.payment.summary||state.payment.submitting)return;if(state.payment.method.indexOf('online:')===0){var checkout=guestCheckoutUrl();if(!checkout)return toast('Customer checkout is unavailable.',true);window.open(checkout,'_blank','noopener');return toast('Customer checkout opened.')}if(state.payment.method==='direct_terminal')return executeTerminalPayment();state.payment.submitting=true;renderPaymentTotals();var summary=state.payment.summary;try{var json=await fetchJson(paymentSettleUrl(),{method:'POST',headers:jsonHeaders(),body:JSON.stringify({idempotency_key:state.payment.idempotencyKey,payment_method:state.payment.method,provider_code:state.payment.method==='external_terminal'?'external_terminal':null,split_mode:state.payment.splitMode,amount:paymentBaseAmount(),selected_items:state.payment.splitMode==='items'?selectedItemPayload():null,tip_amount:paymentTipAmount(),coupon_code:state.payment.coupon?state.payment.coupon.code:null,payer_label:state.payment.payerLabel,payment_reference:state.payment.reference,cash_received:state.payment.method==='cash'?toNumber(state.payment.cashReceived,paymentPayable()):null,external_confirmed:state.payment.method==='external_terminal'?state.payment.externalConfirmed:false,expected_remaining:summary.settlement.remaining_amount,expected_updated_at:summary.order.updated_at})});state.payment.summary=json.summary||state.payment.summary;state.payment.idempotencyKey=uid('pay');renderPayment();toast(json.message||'Payment recorded');showSuccess(json.message||'Payment recorded.');await refreshData(true);if(json.settlement_status==='paid')setTimeout(closePayment,900)}catch(error){toast(error.message||'Payment failed.',true);if(error.status===409||error.status===422)await loadPaymentSummary(true)}finally{state.payment.submitting=false;renderPayment()}}
      async function executeTerminalPayment(){if(!state.payment.providerCode||!state.payment.terminalDeviceId)return toast('Choose a terminal.',true);state.payment.submitting=true;renderPaymentTotals();try{var json=await fetchJson(terminalPaymentUrl(),{method:'POST',headers:jsonHeaders(),body:JSON.stringify({provider_code:state.payment.providerCode,terminal_device_id:state.payment.terminalDeviceId})});state.payment.terminalAttemptId=Number(json.attempt_id)||null;toast(json.message||'Payment sent to terminal.');renderPayment();if(state.payment.terminalAttemptId)await pollTerminalPayment(state.payment.terminalAttemptId)}catch(error){toast(error.message||'Could not send payment to terminal.',true)}finally{state.payment.submitting=false;renderPayment()}}
      async function refreshTerminalAttempt(attemptId){return fetchJson(terminalAttemptRefreshUrl(attemptId),{method:'POST',headers:jsonHeaders(),body:'{}'})}
      async function latestTerminalAttemptId(){var json=await fetchJson(terminalAttemptsUrl()+'?_='+Date.now()),attempts=json.attempts||[];return attempts.length?Number(attempts[0].id)||null:null}
      async function finishTerminalStatus(result,silent){var status=String(result&&result.status?result.status:'').toLowerCase();if(status==='paid'){await loadPaymentSummary(true);await refreshData(true);toast('Payment approved by SumUp.');showSuccess('Payment approved by SumUp.');setTimeout(closePayment,850);return true}if(status==='failed'||status==='cancelled'||status==='reconciliation_required'){await loadPaymentSummary(true);toast((result&&result.message)||'Terminal payment failed.',true);return true}if(!silent)toast('Payment is still waiting on the terminal.');return false}
      async function pollTerminalPayment(attemptId){state.payment.terminalAttemptId=Number(attemptId);for(var i=0;i<45;i++){await wait(2000);var result=await refreshTerminalAttempt(attemptId);if(await finishTerminalStatus(result,true))return result}toast('Payment is still processing. You can refresh the status.',true);return null}
      async function refreshTerminalPaymentStatus(){try{var attemptId=state.payment.terminalAttemptId||await latestTerminalAttemptId();if(!attemptId)return loadPaymentSummary(false);state.payment.terminalAttemptId=attemptId;var result=await refreshTerminalAttempt(attemptId);if(!(await finishTerminalStatus(result,false)))await loadPaymentSummary(true)}catch(error){toast(error.message||'Could not refresh terminal payment.',true)}}

      function bindPayment(){var modal=$('[data-pos-payment-modal]'),close=$('[data-pos-payment-close]');if(close)close.onclick=closePayment;if(modal)modal.addEventListener('click',function(event){if(event.target===modal)closePayment()});$$('[data-split-mode]').forEach(function(button){button.onclick=function(){state.payment.splitMode=button.dataset.splitMode;state.payment.coupon=null;state.payment.couponCode='';state.payment.idempotencyKey=uid('pay');renderPayment()}});$$('[data-tip-percent]').forEach(function(button){button.onclick=function(){state.payment.tipPercent=button.dataset.tipPercent==='custom'?'custom':Number(button.dataset.tipPercent);renderPayment()}});var customTip=$('[data-pos-custom-tip]');if(customTip)customTip.oninput=function(){state.payment.customTip=customTip.value;renderPaymentTotals()};var couponCode=$('[data-pos-coupon-code]');if(couponCode)couponCode.oninput=function(){state.payment.couponCode=couponCode.value.toUpperCase();state.payment.coupon=null;renderPaymentTotals()};var couponApply=$('[data-pos-coupon-apply]');if(couponApply)couponApply.onclick=applyCoupon;var payer=$('[data-pos-payer-label]');if(payer)payer.oninput=function(){state.payment.payerLabel=payer.value};var reference=$('[data-pos-payment-reference]');if(reference)reference.oninput=function(){state.payment.reference=reference.value;renderPaymentTotals()};var cash=$('[data-pos-cash-received]');if(cash)cash.oninput=function(){state.payment.cashReceived=cash.value;renderPaymentTotals()};var external=$('[data-pos-external-confirm]');if(external)external.onchange=function(){state.payment.externalConfirmed=external.checked;renderPaymentTotals()};var pay=$('[data-pos-pay-button]');if(pay)pay.onclick=executePayment;var copy=$('[data-pos-copy-link]');if(copy)copy.onclick=copyPaymentLink;var refresh=$('[data-pos-refresh-payment]');if(refresh)refresh.onclick=refreshTerminalPaymentStatus}

      return {openPayment:openPayment,closePayment:closePayment,bindPayment:bindPayment,renderPayment:renderPayment,paymentRemaining:paymentRemaining,loadPaymentSummary:loadPaymentSummary};
    }
  };
})();
