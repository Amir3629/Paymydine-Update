/* PayMyDine Waiter POS V2 — reusable standalone + dashboard overlay */
:root {
  --pmd-pos-navy: #061126;
  --pmd-pos-navy-2: #0d1b34;
  --pmd-pos-orange: #ff7a00;
  --pmd-pos-orange-soft: #fff1e6;
  --pmd-pos-blue: #1cb7e8;
  --pmd-pos-green: #16a34a;
  --pmd-pos-green-soft: #eaf8ef;
  --pmd-pos-amber: #d97706;
  --pmd-pos-red: #ef3340;
  --pmd-pos-bg: #eef3f8;
  --pmd-pos-card: #ffffff;
  --pmd-pos-line: #d7e0ea;
  --pmd-pos-muted: #64748b;
  --pmd-pos-shadow: 0 18px 50px rgba(6,17,38,.12);
}

.pmd-waiter-pos-page,
.pmd-waiter-pos-page * ,
[data-pmd-pos-root],
[data-pmd-pos-root] * { box-sizing: border-box; }

html, body.pmd-waiter-pos-page { min-height: 100%; }
body.pmd-waiter-pos-page {
  margin: 0;
  background: var(--pmd-pos-bg);
  color: var(--pmd-pos-navy);
  font-family: Inter, Roboto, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  overflow: hidden;
}

[data-pmd-pos-root] button,
[data-pmd-pos-root] input,
[data-pmd-pos-root] textarea { font: inherit; }
[data-pmd-pos-root] button { -webkit-tap-highlight-color: transparent; }
[data-pmd-pos-root] button:focus-visible,
[data-pmd-pos-root] input:focus-visible,
[data-pmd-pos-root] textarea:focus-visible { outline: 3px solid rgba(28,183,232,.22); outline-offset: 2px; }

.pmd-pos-dashboard-overlay {
  position: fixed;
  inset: 0;
  z-index: 2147483000;
  background: var(--pmd-pos-bg);
  animation: pmd-pos-overlay-in .18s ease-out;
}
.pmd-pos-dashboard-overlay.is-closing { animation: pmd-pos-overlay-out .14s ease-in forwards; }
html.pmd-pos-overlay-open,
body.pmd-pos-overlay-open { overflow: hidden !important; }
@keyframes pmd-pos-overlay-in { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: none; } }
@keyframes pmd-pos-overlay-out { to { opacity: 0; transform: translateY(8px); } }

.pmd-pos-app {
  height: 100dvh;
  min-height: 0;
  display: grid;
  grid-template-rows: auto auto minmax(0,1fr);
  background: var(--pmd-pos-bg);
  color: var(--pmd-pos-navy);
  font-family: Inter, Roboto, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
.pmd-pos-success {
  padding: 10px 18px;
  background: var(--pmd-pos-green-soft);
  color: #116329;
  border-bottom: 1px solid #bce7ca;
  font-size: 13px;
  font-weight: 850;
  text-align: center;
}

.pmd-pos-topbar {
  min-height: 76px;
  padding: 12px 18px;
  display: grid;
  grid-template-columns: minmax(220px,1fr) auto minmax(220px,1fr);
  align-items: center;
  gap: 16px;
  background: var(--pmd-pos-navy);
  color: #fff;
  box-shadow: 0 10px 32px rgba(6,17,38,.24);
  z-index: 30;
}
.pmd-pos-top-left,
.pmd-pos-top-right { display: flex; align-items: center; gap: 10px; min-width: 0; }
.pmd-pos-top-right { justify-content: flex-end; }
.pmd-pos-back,
.pmd-pos-icon-btn,
.pmd-pos-ghost-btn {
  border: 1px solid rgba(255,255,255,.18);
  background: rgba(255,255,255,.08);
  color: #fff;
  min-height: 44px;
  border-radius: 13px;
  padding: 0 14px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  font-weight: 850;
  cursor: pointer;
}
.pmd-pos-back:hover,
.pmd-pos-icon-btn:hover,
.pmd-pos-ghost-btn:hover { background: rgba(255,255,255,.14); }
.pmd-pos-icon-btn { width: 44px; padding: 0; }
.pmd-pos-table-title { text-align: center; min-width: 0; }
.pmd-pos-table-title strong { display: block; font-size: clamp(20px,2.2vw,30px); letter-spacing: -.035em; }
.pmd-pos-table-title span { display: block; margin-top: 3px; color: rgba(255,255,255,.66); font-size: 12px; font-weight: 750; }
.pmd-pos-status-dot { width: 9px; height: 9px; border-radius: 99px; background: var(--pmd-pos-green); box-shadow: 0 0 0 4px rgba(34,197,94,.14); }
.pmd-pos-waiter { min-width: 0; color: rgba(255,255,255,.82); font-weight: 800; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.pmd-pos-mobile-only { display: none !important; }

.pmd-pos-workspace {
  min-height: 0;
  display: grid;
  grid-template-columns: minmax(0,1fr) minmax(360px,410px);
  gap: 14px;
  padding: 14px;
}
.pmd-pos-catalog,
.pmd-pos-cart {
  min-height: 0;
  background: var(--pmd-pos-card);
  border: 1px solid var(--pmd-pos-line);
  border-radius: 22px;
  box-shadow: var(--pmd-pos-shadow);
  overflow: hidden;
}
.pmd-pos-catalog { display: grid; grid-template-rows: auto auto auto minmax(0,1fr); }
.pmd-pos-tools { padding: 14px; display: grid; grid-template-columns: minmax(220px,1fr) auto; gap: 10px; border-bottom: 1px solid var(--pmd-pos-line); }
.pmd-pos-search-wrap { position: relative; }
.pmd-pos-search {
  width: 100%; min-height: 48px; border: 1px solid var(--pmd-pos-line); border-radius: 14px;
  padding: 0 44px 0 16px; outline: 0; background: #f8fafc; color: var(--pmd-pos-navy); font-weight: 750;
}
.pmd-pos-search:focus { border-color: var(--pmd-pos-blue); box-shadow: 0 0 0 4px rgba(28,183,232,.12); background: #fff; }
.pmd-pos-search-icon { position: absolute; right: 15px; top: 50%; transform: translateY(-50%); opacity: .5; pointer-events: none; }
.pmd-pos-view-toggle { display: flex; gap: 6px; padding: 4px; background: #eef3f8; border-radius: 14px; }
.pmd-pos-view-toggle button { width: 40px; min-height: 40px; border: 0; border-radius: 10px; background: transparent; cursor: pointer; font-weight: 900; color: var(--pmd-pos-navy); }
.pmd-pos-view-toggle button.is-active { background: #fff; box-shadow: 0 4px 12px rgba(6,17,38,.10); }
.pmd-pos-warning { margin: 10px 14px 0; padding: 9px 12px; border-radius: 12px; background: #fff7ed; border: 1px solid #fed7aa; color: #9a4b00; font-size: 12px; font-weight: 750; }

.pmd-pos-categories { padding: 0 14px 12px; display: flex; gap: 8px; overflow-x: auto; scrollbar-width: thin; border-bottom: 1px solid var(--pmd-pos-line); }
.pmd-pos-category { flex: 0 0 auto; min-height: 40px; padding: 0 16px; border-radius: 999px; border: 1px solid var(--pmd-pos-line); background: #fff; color: var(--pmd-pos-navy); font-weight: 850; cursor: pointer; }
.pmd-pos-category.is-active { color: #fff; background: var(--pmd-pos-navy); border-color: var(--pmd-pos-navy); }

.pmd-pos-menu-scroll { min-height: 0; overflow: auto; padding: 14px; }
.pmd-pos-menu-grid { display: grid; grid-template-columns: repeat(auto-fill,minmax(155px,1fr)); gap: 10px; align-content: start; }
.pmd-pos-menu-grid.is-list { grid-template-columns: repeat(auto-fill,minmax(290px,1fr)); }
.pmd-pos-product {
  min-height: 132px; border: 1px solid var(--pmd-pos-line); border-radius: 17px; padding: 14px;
  background: linear-gradient(180deg,#fff 0%,#f8fafc 100%); color: var(--pmd-pos-navy);
  text-align: left; cursor: pointer; display: flex; flex-direction: column; justify-content: space-between;
  transition: transform .12s ease,border-color .12s ease,box-shadow .12s ease;
}
.pmd-pos-menu-grid.is-list .pmd-pos-product { min-height: 92px; display: grid; grid-template-columns: minmax(0,1fr) auto; align-items: center; gap: 14px; }
.pmd-pos-menu-grid.is-list .pmd-pos-product-foot { margin: 0; align-items: center; }
.pmd-pos-product:hover,
.pmd-pos-product:focus { transform: translateY(-1px); border-color: #9fb4c9; box-shadow: 0 12px 26px rgba(6,17,38,.10); outline: none; }
.pmd-pos-product:active { transform: scale(.985); }
.pmd-pos-product-name { font-size: 16px; line-height: 1.12; font-weight: 950; letter-spacing: -.025em; }
.pmd-pos-product-desc { color: var(--pmd-pos-muted); font-size: 11px; line-height: 1.3; margin-top: 5px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
.pmd-pos-product-foot { margin-top: 12px; display: flex; align-items: end; justify-content: space-between; gap: 8px; }
.pmd-pos-price { color: var(--pmd-pos-orange); font-size: 17px; font-weight: 950; }
.pmd-pos-plus { width: 31px; height: 31px; border-radius: 10px; display: grid; place-items: center; background: var(--pmd-pos-navy); color: #fff; font-size: 20px; font-weight: 900; }
.pmd-pos-product.has-options .pmd-pos-plus { background: var(--pmd-pos-blue); }
.pmd-pos-empty { grid-column: 1/-1; min-height: 220px; display: grid; place-items: center; text-align: center; color: var(--pmd-pos-muted); font-weight: 800; }

.pmd-pos-cart { display: grid; grid-template-rows: auto auto auto minmax(0,1fr) auto; }
.pmd-pos-cart-head { padding: 16px 16px 12px; border-bottom: 1px solid var(--pmd-pos-line); }
.pmd-pos-cart-head-row { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.pmd-pos-cart-head h2 { margin: 0; font-size: 22px; letter-spacing: -.035em; }
.pmd-pos-order-pill { min-height: 31px; padding: 0 11px; border-radius: 999px; display: inline-flex; align-items: center; background: #e8f8fd; color: #036783; font-size: 12px; font-weight: 900; }
.pmd-pos-order-pill.is-partial { background: #fff7ed; color: #9a4b00; }
.pmd-pos-order-pill.is-paid { background: var(--pmd-pos-green-soft); color: #116329; }
.pmd-pos-guest-row { margin-top: 12px; display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.pmd-pos-guest-row small { color: var(--pmd-pos-muted); }
.pmd-pos-stepper { display: inline-flex; align-items: center; gap: 9px; padding: 4px; border-radius: 12px; background: #eef3f8; }
.pmd-pos-stepper button { width: 34px; height: 34px; border: 0; border-radius: 9px; background: #fff; cursor: pointer; font-weight: 950; font-size: 17px; color: var(--pmd-pos-navy); }
.pmd-pos-stepper span { min-width: 26px; text-align: center; font-weight: 950; }
.pmd-pos-existing { display: none; padding: 10px 16px; background: #fff7ed; border-bottom: 1px solid #fed7aa; color: #9a4b00; font-size: 12px; font-weight: 800; }
.pmd-pos-existing.is-show { display: block; }
.pmd-pos-section-heading { padding: 9px 13px; display: flex; align-items: center; justify-content: space-between; gap: 10px; background: #f8fafc; border-bottom: 1px solid var(--pmd-pos-line); font-size: 12px; }
.pmd-pos-section-heading span { font-weight: 950; text-transform: uppercase; letter-spacing: .06em; }
.pmd-pos-section-heading small { color: var(--pmd-pos-muted); font-weight: 750; }
.pmd-pos-sent-section { border-bottom: 1px solid var(--pmd-pos-line); max-height: 210px; overflow: auto; }
.pmd-pos-sent-list { padding: 7px 12px; }
.pmd-pos-sent-item { padding: 9px 0; display: grid; grid-template-columns: auto minmax(0,1fr) auto; gap: 9px; align-items: start; border-bottom: 1px dashed #e6ebf0; }
.pmd-pos-sent-item:last-child { border-bottom: 0; }
.pmd-pos-sent-qty { min-width: 27px; height: 27px; display: grid; place-items: center; border-radius: 9px; background: #eef3f8; font-size: 12px; font-weight: 950; }
.pmd-pos-sent-name { font-size: 13px; font-weight: 850; }
.pmd-pos-sent-name small { display: block; margin-top: 3px; color: var(--pmd-pos-muted); font-weight: 650; }
.pmd-pos-sent-price { white-space: nowrap; font-size: 12px; font-weight: 900; }
.pmd-pos-new-section { min-height: 0; display: grid; grid-template-rows: auto minmax(0,1fr); }
.pmd-pos-cart-list { min-height: 0; overflow: auto; padding: 10px 12px; }
.pmd-pos-cart-empty { min-height: 150px; display: grid; place-items: center; text-align: center; color: var(--pmd-pos-muted); padding: 20px; }
.pmd-pos-line-item { padding: 12px; margin-bottom: 8px; border: 1px solid var(--pmd-pos-line); border-radius: 15px; background: #f8fafc; }
.pmd-pos-line-top { display: flex; align-items: start; justify-content: space-between; gap: 10px; }
.pmd-pos-line-name { min-width: 0; font-weight: 900; line-height: 1.2; }
.pmd-pos-line-options { margin-top: 4px; color: var(--pmd-pos-muted); font-size: 11px; line-height: 1.35; }
.pmd-pos-line-price { white-space: nowrap; font-weight: 950; }
.pmd-pos-line-actions { margin-top: 10px; display: flex; align-items: center; justify-content: space-between; gap: 8px; }
.pmd-pos-qty { display: inline-flex; align-items: center; gap: 4px; }
.pmd-pos-qty button,
.pmd-pos-remove { width: 34px; height: 34px; border: 1px solid var(--pmd-pos-line); border-radius: 10px; background: #fff; cursor: pointer; font-weight: 950; color: var(--pmd-pos-navy); }
.pmd-pos-qty b { width: 30px; text-align: center; }
.pmd-pos-remove { color: var(--pmd-pos-red); }
.pmd-pos-note-input { width: 100%; margin-top: 9px; min-height: 34px; border: 1px solid var(--pmd-pos-line); border-radius: 10px; padding: 7px 10px; background: #fff; outline: 0; font-size: 12px; }

.pmd-pos-checkout { border-top: 1px solid var(--pmd-pos-line); padding: 12px 14px 14px; background: #fff; }
.pmd-pos-table-note { width: 100%; min-height: 42px; max-height: 74px; resize: vertical; border: 1px solid var(--pmd-pos-line); border-radius: 12px; padding: 10px 12px; outline: 0; background: #f8fafc; }
.pmd-pos-totals { margin-top: 10px; display: grid; gap: 5px; }
.pmd-pos-total-row { display: flex; justify-content: space-between; gap: 10px; color: var(--pmd-pos-muted); font-size: 13px; font-weight: 800; }
.pmd-pos-total-row.grand { margin-top: 4px; padding-top: 9px; border-top: 1px solid var(--pmd-pos-line); color: var(--pmd-pos-navy); font-size: 20px; font-weight: 950; }
.pmd-pos-actions { margin-top: 12px; display: grid; grid-template-columns: 1fr 1.45fr; gap: 8px; }
.pmd-pos-action { min-height: 52px; border: 0; border-radius: 14px; cursor: pointer; font-weight: 950; display: flex; align-items: center; justify-content: center; gap: 3px; flex-direction: column; }
.pmd-pos-action small { font-size: 10px; font-weight: 750; opacity: .65; }
.pmd-pos-action.hold { background: #e8eef5; color: var(--pmd-pos-navy); }
.pmd-pos-action.send { background: var(--pmd-pos-orange); color: #fff; box-shadow: 0 10px 24px rgba(255,122,0,.24); }
.pmd-pos-action:disabled { opacity: 1; background: #e8edf3; color: #94a3b8; cursor: not-allowed; box-shadow: none; }
.pmd-pos-secondary-actions { margin-top: 8px; display: grid; grid-template-columns: repeat(3,1fr); gap: 7px; }
.pmd-pos-secondary-actions button { min-height: 40px; border: 1px solid var(--pmd-pos-line); border-radius: 11px; background: #fff; color: var(--pmd-pos-navy); cursor: pointer; font-weight: 850; }
.pmd-pos-secondary-actions button:disabled { background: #f4f6f8; color: #a0aec0; cursor: not-allowed; }

.pmd-pos-mobile-cart-bar { display: none; }

.pmd-pos-modal,
.pmd-pos-payment-modal { position: fixed; inset: 0; z-index: 2147483100; display: none; align-items: center; justify-content: center; padding: 18px; background: rgba(6,17,38,.58); backdrop-filter: blur(7px); }
.pmd-pos-modal.is-show,
.pmd-pos-payment-modal.is-show { display: flex; }
.pmd-pos-modal-card { width: min(560px,100%); max-height: min(82dvh,760px); display: grid; grid-template-rows: auto minmax(0,1fr) auto; border-radius: 20px; overflow: hidden; background: #fff; box-shadow: 0 30px 90px rgba(6,17,38,.28); }
.pmd-pos-modal-head,
.pmd-pos-modal-foot { padding: 14px 16px; display: flex; align-items: center; justify-content: space-between; gap: 10px; border-bottom: 1px solid var(--pmd-pos-line); }
.pmd-pos-modal-foot { border-top: 1px solid var(--pmd-pos-line); border-bottom: 0; }
.pmd-pos-modal-head h2 { margin: 0; font-size: 21px; }
.pmd-pos-modal-close,
.pmd-pos-payment-close { width: 42px; height: 42px; border: 1px solid var(--pmd-pos-line); border-radius: 13px; background: #eef3f8; color: var(--pmd-pos-navy); cursor: pointer; font-size: 21px; font-weight: 900; }
.pmd-pos-modal-body { min-height: 0; overflow: auto; padding: 14px 16px; }
.pmd-pos-option-group { padding: 13px; border: 1px solid var(--pmd-pos-line); border-radius: 15px; margin-bottom: 10px; }
.pmd-pos-option-title { display: flex; align-items: center; justify-content: space-between; gap: 10px; font-weight: 900; }
.pmd-pos-option-title small { color: var(--pmd-pos-orange); }
.pmd-pos-option-list { margin-top: 9px; display: grid; gap: 7px; }
.pmd-pos-option { min-height: 42px; padding: 8px 10px; border-radius: 11px; display: flex; align-items: center; justify-content: space-between; gap: 10px; background: #f8fafc; cursor: pointer; }
.pmd-pos-modal-cancel,
.pmd-pos-modal-add { min-height: 44px; border: 0; border-radius: 12px; padding: 0 18px; cursor: pointer; font-weight: 900; }
.pmd-pos-modal-cancel { background: #e8eef5; color: var(--pmd-pos-navy); }
.pmd-pos-modal-add { background: var(--pmd-pos-orange); color: #fff; }

.pmd-pos-payment-dialog { width: min(1120px,100%); height: min(90dvh,850px); display: grid; grid-template-rows: auto minmax(0,1fr); overflow: hidden; border-radius: 24px; background: #f4f7fa; box-shadow: 0 34px 100px rgba(6,17,38,.34); }
.pmd-pos-payment-head { padding: 17px 20px; display: flex; align-items: center; justify-content: space-between; gap: 16px; background: var(--pmd-pos-navy); color: #fff; }
.pmd-pos-payment-head h2 { margin: 2px 0 0; font-size: 25px; letter-spacing: -.035em; }
.pmd-pos-payment-head p { margin: 4px 0 0; color: rgba(255,255,255,.65); font-size: 12px; font-weight: 700; }
.pmd-pos-payment-eyebrow { color: #7dd3fc; font-size: 10px; letter-spacing: .14em; font-weight: 950; }
.pmd-pos-payment-close { border-color: rgba(255,255,255,.18); background: rgba(255,255,255,.08); color: #fff; }
.pmd-pos-payment-body { min-height: 0; display: grid; grid-template-columns: minmax(0,1fr) 340px; gap: 14px; padding: 14px; }
.pmd-pos-payment-main { min-height: 0; overflow: auto; display: grid; gap: 12px; align-content: start; padding-right: 3px; }
.pmd-pos-payment-summary { min-height: 0; overflow: auto; padding: 16px; border: 1px solid var(--pmd-pos-line); border-radius: 18px; background: #fff; box-shadow: 0 12px 32px rgba(6,17,38,.08); }
.pmd-pos-payment-summary h3 { margin: 0 0 12px; font-size: 20px; }
.pmd-pos-payment-balance { display: grid; grid-template-columns: repeat(3,1fr); gap: 9px; }
.pmd-pos-balance-card { padding: 13px; border: 1px solid var(--pmd-pos-line); border-radius: 15px; background: #fff; }
.pmd-pos-balance-card span { display: block; color: var(--pmd-pos-muted); font-size: 10px; text-transform: uppercase; letter-spacing: .07em; font-weight: 850; }
.pmd-pos-balance-card b { display: block; margin-top: 5px; font-size: 20px; }
.pmd-pos-balance-card.is-remaining { border-color: #fed7aa; background: #fff7ed; }
.pmd-pos-payment-block { padding: 14px; border: 1px solid var(--pmd-pos-line); border-radius: 17px; background: #fff; }
.pmd-pos-payment-block-title { display: flex; align-items: baseline; justify-content: space-between; gap: 10px; margin-bottom: 10px; }
.pmd-pos-payment-block-title b { font-size: 14px; }
.pmd-pos-payment-block-title span { color: var(--pmd-pos-muted); font-size: 10px; font-weight: 700; }
.pmd-pos-split-tabs { display: grid; grid-template-columns: repeat(4,1fr); gap: 7px; }
.pmd-pos-split-tabs button { min-height: 40px; border: 1px solid var(--pmd-pos-line); border-radius: 12px; background: #f8fafc; color: var(--pmd-pos-navy); cursor: pointer; font-weight: 850; }
.pmd-pos-split-tabs button.is-active { background: var(--pmd-pos-navy); color: #fff; border-color: var(--pmd-pos-navy); }
.pmd-pos-split-panel { margin-top: 11px; }
.pmd-pos-split-equal { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 11px; border-radius: 13px; background: #f8fafc; }
.pmd-pos-split-equal strong { display: block; }
.pmd-pos-split-equal small { color: var(--pmd-pos-muted); }
.pmd-pos-split-stepper { display: inline-flex; align-items: center; gap: 8px; }
.pmd-pos-split-stepper button { width: 34px; height: 34px; border: 1px solid var(--pmd-pos-line); border-radius: 10px; background: #fff; cursor: pointer; font-weight: 950; }
.pmd-pos-split-items { max-height: 250px; overflow: auto; display: grid; gap: 7px; }
.pmd-pos-split-item { display: grid; grid-template-columns: minmax(0,1fr) auto auto; gap: 10px; align-items: center; padding: 10px; border: 1px solid var(--pmd-pos-line); border-radius: 12px; background: #f8fafc; }
.pmd-pos-split-item-name { min-width: 0; font-size: 12px; font-weight: 850; }
.pmd-pos-split-item-name small { display: block; color: var(--pmd-pos-muted); font-weight: 650; margin-top: 2px; }
.pmd-pos-item-pay-qty { width: 70px; min-height: 36px; border: 1px solid var(--pmd-pos-line); border-radius: 10px; padding: 0 8px; }
.pmd-pos-custom-row { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 10px; align-items: end; }
.pmd-pos-method-grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(145px,1fr)); gap: 8px; }
.pmd-pos-method { min-height: 66px; padding: 10px 12px; border: 1px solid var(--pmd-pos-line); border-radius: 14px; background: #f8fafc; color: var(--pmd-pos-navy); text-align: left; cursor: pointer; }
.pmd-pos-method b { display: block; font-size: 13px; }
.pmd-pos-method small { display: block; margin-top: 4px; color: var(--pmd-pos-muted); font-size: 10px; line-height: 1.25; }
.pmd-pos-method.is-active { border-color: var(--pmd-pos-orange); box-shadow: 0 0 0 3px rgba(255,122,0,.13); background: var(--pmd-pos-orange-soft); }
.pmd-pos-online-box,
.pmd-pos-terminal-box { margin-top: 10px; padding: 11px; border-radius: 13px; background: #eef7fb; border: 1px solid #bae6fd; color: #075985; font-size: 12px; line-height: 1.45; }
.pmd-pos-terminal-provider-row { margin-top: 8px; display: flex; gap: 7px; flex-wrap: wrap; }
.pmd-pos-terminal-provider-row button { min-height: 36px; padding: 0 12px; border: 1px solid #7dd3fc; border-radius: 10px; background: #fff; color: #075985; cursor: pointer; font-weight: 850; }
.pmd-pos-adjustments { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.pmd-pos-tip-buttons { display: grid; grid-template-columns: repeat(4,1fr); gap: 6px; }
.pmd-pos-tip-buttons button { min-height: 38px; border: 1px solid var(--pmd-pos-line); border-radius: 10px; background: #f8fafc; cursor: pointer; font-size: 11px; font-weight: 850; }
.pmd-pos-tip-buttons button.is-active { background: var(--pmd-pos-navy); color: #fff; }
.pmd-pos-payment-input { width: 100%; min-height: 42px; border: 1px solid var(--pmd-pos-line); border-radius: 11px; padding: 0 11px; background: #fff; color: var(--pmd-pos-navy); }
.pmd-pos-custom-tip { margin-top: 7px; }
.pmd-pos-coupon-row { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; }
.pmd-pos-coupon-row button { min-height: 42px; padding: 0 14px; border: 0; border-radius: 11px; background: var(--pmd-pos-navy); color: #fff; cursor: pointer; font-weight: 850; }
.pmd-pos-coupon-result { min-height: 18px; margin-top: 5px; font-size: 11px; font-weight: 750; color: var(--pmd-pos-green); }
.pmd-pos-coupon-result.is-error { color: var(--pmd-pos-red); }
.pmd-pos-payment-fields { display: grid; grid-template-columns: repeat(3,1fr); gap: 9px; }
.pmd-pos-payment-fields label span { display: block; margin-bottom: 5px; color: var(--pmd-pos-muted); font-size: 10px; font-weight: 800; }
.pmd-pos-confirm-row { margin-top: 10px; display: flex; align-items: flex-start; gap: 8px; font-size: 11px; font-weight: 750; }
.pmd-pos-pay-totals { display: grid; gap: 8px; }
.pmd-pos-pay-total-row { display: flex; align-items: center; justify-content: space-between; gap: 10px; color: var(--pmd-pos-muted); font-size: 12px; font-weight: 750; }
.pmd-pos-pay-total-row strong { color: var(--pmd-pos-navy); }
.pmd-pos-pay-total-row.is-grand { margin-top: 5px; padding-top: 12px; border-top: 1px solid var(--pmd-pos-line); color: var(--pmd-pos-navy); font-size: 19px; font-weight: 950; }
.pmd-pos-change-box { margin-top: 12px; padding: 11px; border-radius: 12px; background: var(--pmd-pos-green-soft); color: #116329; text-align: center; font-weight: 900; }
.pmd-pos-pay-button { width: 100%; min-height: 52px; margin-top: 14px; border: 0; border-radius: 14px; background: var(--pmd-pos-orange); color: #fff; cursor: pointer; font-weight: 950; box-shadow: 0 10px 24px rgba(255,122,0,.22); }
.pmd-pos-pay-button:disabled { background: #e8edf3; color: #94a3b8; box-shadow: none; cursor: not-allowed; }
.pmd-pos-payment-secondary { width: 100%; min-height: 41px; margin-top: 7px; border: 1px solid var(--pmd-pos-line); border-radius: 11px; background: #fff; color: var(--pmd-pos-navy); cursor: pointer; font-size: 11px; font-weight: 850; }
.pmd-pos-payment-safety { margin: 10px 0 0; color: var(--pmd-pos-muted); font-size: 9px; line-height: 1.45; text-align: center; }
.pmd-pos-payment-history-wrap { margin-top: 16px; padding-top: 13px; border-top: 1px solid var(--pmd-pos-line); }
.pmd-pos-payment-history { display: grid; gap: 7px; }
.pmd-pos-payment-history-empty { padding: 15px; border-radius: 12px; background: #f8fafc; color: var(--pmd-pos-muted); font-size: 11px; text-align: center; }
.pmd-pos-payment-history-item { padding: 10px; border: 1px solid var(--pmd-pos-line); border-radius: 12px; background: #f8fafc; }
.pmd-pos-payment-history-item > div { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
.pmd-pos-payment-history-item b { font-size: 12px; }
.pmd-pos-payment-history-item small { display: block; margin-top: 4px; color: var(--pmd-pos-muted); font-size: 9px; }
.pmd-pos-payment-history-item a { color: #036783; font-size: 10px; font-weight: 850; text-decoration: none; }

.pmd-pos-toast { position: fixed; left: 50%; bottom: max(20px,env(safe-area-inset-bottom)); z-index: 2147483200; transform: translate(-50%,18px); opacity: 0; pointer-events: none; min-width: min(360px,calc(100vw - 32px)); max-width: 560px; padding: 12px 16px; border-radius: 13px; background: var(--pmd-pos-navy); color: #fff; box-shadow: 0 16px 40px rgba(6,17,38,.24); font-weight: 800; text-align: center; transition: .18s ease; }
.pmd-pos-toast.is-show { transform: translate(-50%,0); opacity: 1; }
.pmd-pos-toast.is-error { background: #b91c1c; }

@media (max-width: 1050px) {
  .pmd-pos-workspace { grid-template-columns: minmax(0,1fr) 350px; }
  .pmd-pos-payment-body { grid-template-columns: minmax(0,1fr) 300px; }
  .pmd-pos-adjustments { grid-template-columns: 1fr; }
  .pmd-pos-payment-fields { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 820px) {
  .pmd-pos-app { grid-template-rows: auto auto minmax(0,1fr) auto; }
  .pmd-pos-topbar { min-height: 66px; grid-template-columns: auto minmax(0,1fr) auto; padding: 9px 10px; }
  .pmd-pos-waiter,.pmd-pos-status-dot,.pmd-pos-ghost-btn { display: none; }
  .pmd-pos-back { min-width: 44px; width: 44px; padding: 0; font-size: 0; }
  .pmd-pos-back::before { content: "←"; font-size: 20px; }
  .pmd-pos-table-title strong { font-size: 20px; }
  .pmd-pos-table-title span { font-size: 10px; }
  .pmd-pos-workspace { display: block; padding: 8px; }
  .pmd-pos-catalog { height: 100%; border-radius: 17px; }
  .pmd-pos-cart { position: fixed; inset: 0; z-index: 2147483050; display: grid; border: 0; border-radius: 0; transform: translateY(100%); transition: transform .22s ease; }
  .pmd-pos-cart.is-mobile-open { transform: translateY(0); }
  .pmd-pos-mobile-only { display: inline-flex !important; background: #eef3f8; color: var(--pmd-pos-navy); border-color: var(--pmd-pos-line); }
  .pmd-pos-mobile-cart-bar { display: flex; min-height: 62px; margin: 0 8px calc(8px + env(safe-area-inset-bottom)); padding: 0 16px; border: 0; border-radius: 16px; align-items: center; justify-content: space-between; gap: 12px; background: var(--pmd-pos-navy); color: #fff; box-shadow: 0 14px 34px rgba(6,17,38,.25); cursor: pointer; }
  .pmd-pos-mobile-cart-bar span:first-child { display: flex; flex-direction: column; align-items: flex-start; }
  .pmd-pos-mobile-cart-bar small { color: rgba(255,255,255,.65); }
  .pmd-pos-mobile-cart-total { font-size: 18px; font-weight: 950; }
  .pmd-pos-menu-grid { grid-template-columns: repeat(2,minmax(0,1fr)); }
  .pmd-pos-menu-grid.is-list { grid-template-columns: 1fr; }
  .pmd-pos-product { min-height: 125px; padding: 12px; }
  .pmd-pos-tools { padding: 10px; }
  .pmd-pos-categories { padding: 0 10px 10px; }
  .pmd-pos-menu-scroll { padding: 10px; }
  .pmd-pos-payment-modal { padding: 0; align-items: stretch; }
  .pmd-pos-payment-dialog { width: 100%; height: 100dvh; border-radius: 0; }
  .pmd-pos-payment-body { display: block; overflow: auto; padding: 10px; }
  .pmd-pos-payment-main { overflow: visible; }
  .pmd-pos-payment-summary { margin-top: 10px; overflow: visible; }
  .pmd-pos-payment-balance { grid-template-columns: 1fr 1fr 1fr; }
  .pmd-pos-split-tabs { grid-template-columns: 1fr 1fr; }
  .pmd-pos-payment-fields { grid-template-columns: 1fr; }
}

@media (max-width: 520px) {
  .pmd-pos-top-right .pmd-pos-icon-btn { width: 40px; min-height: 40px; }
  .pmd-pos-product-name { font-size: 14px; }
  .pmd-pos-product-desc { font-size: 10px; }
  .pmd-pos-price { font-size: 15px; }
  .pmd-pos-categories { gap: 6px; }
  .pmd-pos-category { min-height: 37px; padding: 0 13px; font-size: 12px; }
  .pmd-pos-view-toggle { display: none; }
  .pmd-pos-tools { grid-template-columns: 1fr; }
  .pmd-pos-payment-head { padding: 13px 14px; }
  .pmd-pos-payment-head h2 { font-size: 21px; }
  .pmd-pos-payment-balance { grid-template-columns: 1fr; }
  .pmd-pos-balance-card { display: flex; align-items: center; justify-content: space-between; }
  .pmd-pos-balance-card b { margin: 0; }
  .pmd-pos-method-grid { grid-template-columns: 1fr 1fr; }
  .pmd-pos-adjustments { gap: 12px; }
  .pmd-pos-tip-buttons { grid-template-columns: 1fr 1fr; }
  .pmd-pos-payment-block { padding: 12px; }
  .pmd-pos-modal { padding: 0; align-items: flex-end; }
  .pmd-pos-modal-card { max-height: 92dvh; border-radius: 22px 22px 0 0; }
}
