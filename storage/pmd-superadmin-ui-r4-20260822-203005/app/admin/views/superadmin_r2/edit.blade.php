@extends('admin::superadmin_r2.layout')
@section('title','Edit Restaurant')
@section('page_title','Edit Restaurant')
@section('page_subtitle','Update tenant metadata without changing database or domain identity')
@section('content')
<div class="hero"><div><h2>{{ $tenant->name }}</h2><p>{{ $tenant->domain }} · Database: {{ $tenant->database }}</p></div><a class="btn btn-soft" href="/superadmin/new">Back to restaurants</a></div>
<div class="card" style="max-width:860px"><div class="card-head"><div><h3>Restaurant details</h3><p>Domain and database are immutable here to avoid accidental tenant identity changes.</p></div></div><form method="POST" action="/superadmin/tenants/update">@csrf<input type="hidden" name="id" value="{{ $tenant->id }}"><div class="field-grid"><div class="field"><label>Restaurant name</label><input name="name" value="{{ old('name',$tenant->name) }}" required></div><div class="field"><label>Email</label><input type="email" name="email" value="{{ old('email',$tenant->email) }}" required></div><div class="field"><label>Phone</label><input name="phone" value="{{ old('phone',$tenant->phone) }}" required></div><div class="field"><label>Country</label><input name="country" value="{{ old('country',$tenant->country) }}" required></div><div class="field"><label>Start date</label><input type="date" name="start" value="{{ old('start',$tenant->start) }}" required></div><div class="field"><label>End date</label><input type="date" name="end" value="{{ old('end',$tenant->end) }}" required></div><div class="field"><label>Plan / type</label><input name="type" value="{{ old('type',$tenant->type) }}" required></div><div class="field full"><label>Description</label><textarea name="description">{{ old('description',$tenant->description) }}</textarea></div><div class="field full"><button class="btn btn-primary" type="submit">Save restaurant</button></div></div></form></div>
@endsection
