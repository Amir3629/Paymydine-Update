{
  "name": "paymydine-cashier-desktop",
  "version": "1.0.4",
  "private": true,
  "description": "PayMyDine universal cashier desktop app with physical printer, cash drawer and Virtual PDF print-debug support for Windows and macOS",
  "main": "src/main.js",
  "scripts": {
    "patch:v104": "node scripts/apply-v104.js",
    "dev": "npm run patch:v104 && electron .",
    "start": "npm run patch:v104 && electron .",
    "check": "npm run patch:v104 && node --check src/main.js && node --check src/preload.js && node --check src/setup.js && node --check src/hardware.js && node --check src/receipt.js && node --check src/hardware-page.js && node --check src/offline.js",
    "dist:win": "npm run patch:v104 && electron-builder --win nsis",
    "dist:mac": "npm run patch:v104 && electron-builder --mac dmg zip"
  },
  "devDependencies": {
    "electron": "43.4.1",
    "electron-builder": "26.15.7"
  },
  "build": {
    "appId": "com.paymydine.cashier",
    "productName": "PayMyDine Cashier",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": ["nsis"],
      "artifactName": "PayMyDine-Cashier-Setup-${version}.${ext}"
    },
    "mac": {
      "target": ["dmg", "zip"],
      "category": "public.app-category.business",
      "artifactName": "PayMyDine-Cashier-${version}-mac-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "PayMyDine Cashier"
    }
  }
}
