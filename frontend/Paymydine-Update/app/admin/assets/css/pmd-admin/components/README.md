# Components

Reusable admin components belong here when they can be safely expressed with `.pmd-` prefixed classes.

Do not target global Bootstrap/TastyIgniter selectors directly. For example, avoid `.btn`, `.btn-primary`, `.form-control`, `.modal`, `.table`, and `.dropdown-menu` in this folder.

Starter components in this folder are intentionally small and are not loaded by the admin layout yet.
