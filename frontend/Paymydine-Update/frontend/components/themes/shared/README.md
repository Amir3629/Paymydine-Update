# Shared theme UI primitives

This folder is for UI-only primitives that can be reused by multiple theme implementations without carrying business logic.

Shared behavior should live in `frontend/features/*`. Components in this folder should receive typed action props and render state only.
