# Organic Botanical Paper theme components

This folder is reserved for the future native Organic Botanical Paper UI.

The current Organic experience still uses the v0 iframe prototype. Do not migrate or remove that iframe in Phase 3A. Future native Organic components should connect to shared PayMyDine behavior through the theme action contracts instead of duplicating checkout, order, payment, waiter, note, or valet logic.
