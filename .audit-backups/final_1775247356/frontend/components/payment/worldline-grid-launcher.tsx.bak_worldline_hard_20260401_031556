"use client"

import { Button } from "@/components/ui/button"

export default function WorldlineGridLauncher() {
  return (
    <div className="w-full mt-4" data-pmd-worldline-grid-launcher="1">
      <div className="rounded-2xl border p-4 text-center space-y-3">
        <div className="text-sm font-semibold">Worldline Hosted Checkout</div>
        <div className="text-xs opacity-70">
          Click below to continue to the Worldline hosted payment step.
        </div>
        <Button
          type="button"
          className="w-full rounded-full"
          onClick={() => {
            window.location.href = "/worldline-test"
          }}
        >
          Continue with Worldline
        </Button>
      </div>
    </div>
  )
}
